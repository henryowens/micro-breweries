import { css } from "@emotion/react";

const homeStyles = {
  divider: css({
    borderLeftWidth: "1px",
    height: "inherit",
  }),
  toggleContainer: css({
    width:"auto",
    height:"fit-content"
  })
};

export default homeStyles;
